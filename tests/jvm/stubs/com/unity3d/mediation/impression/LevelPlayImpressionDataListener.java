package com.unity3d.mediation.impression;

/** Minimal stand-in for mediation-sdk 9.3.0 {@code LevelPlayImpressionDataListener}. */
public interface LevelPlayImpressionDataListener {
    void onImpressionSuccess(LevelPlayImpressionData impressionData);
}
